using UnityEngine;

public class test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Managers go = Managers.Instance;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
